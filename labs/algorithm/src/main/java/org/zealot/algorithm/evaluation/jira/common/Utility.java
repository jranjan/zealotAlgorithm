package org.zealot.algorithm.evaluation.jira.common;

public class Utility {
    static void swap(int[] arr, int i, int j)
    {
        int temp = arr[i];
        arr[i] = arr[j];
        arr[j] = temp;
    }
}
