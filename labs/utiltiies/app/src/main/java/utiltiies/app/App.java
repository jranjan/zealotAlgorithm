/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package utiltiies.app;

import utiltiies.list.LinkedList;

import static utiltiies.utilities.StringUtils.join;
import static utiltiies.utilities.StringUtils.split;
import static utiltiies.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
