/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package labs.java.app;

import labs.java.list.LinkedList;

import static labs.java.utilities.StringUtils.join;
import static labs.java.utilities.StringUtils.split;
import static labs.java.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
